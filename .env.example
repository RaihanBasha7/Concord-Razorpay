[TEMPLATE]
GROQ_API_KEY=

# CORS — comma-separated list of allowed origins, or "*" for development.
# Leave empty to block all cross-origin browser requests (safe default).
# CONCORD_CORS_ORIGINS=https://app.example.com
